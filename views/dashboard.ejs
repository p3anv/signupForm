<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        /* General styling */
        body {
            font-family: sans-serif;
            font-size: 16px;
            background-color: #f5f5f5;
            margin: 0;
            padding: 20px;
        }

        /* Heading and content container */
        #main-content {
            background-color: #fff;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        /* Heading */
        h1 {
            font-size: 24px;
            margin-bottom: 20px;
        }

        /* User information container */
        #userInfo {
            margin-bottom: 20px;
        }

        #userInfo p {
            margin-bottom: 10px;
        }

        #userInfo strong {
            font-weight: bold;
            margin-right: 10px;
        }

        /* Logout link */
        a {
            color: #007bff;
            text-decoration: none;
            padding: 10px 20px;
            border: 1px solid #007bff;
            border-radius: 5px;
            margin-top: 20px;
        }

        a:hover {
            background-color: #007bff;
            color: #fff;
        }
    </style>
</head>
<body>
    <div id="main-content">
        <h1>Welcome to the Dashboard</h1>
        <div id="userInfo">
            <p><strong>Username:</strong> <span id="username"><%= user.username %></span></p>
            <p><strong>Email:</strong> <span id="email"><%= user.email %></span></p>
            <!-- Note: It's not recommended to display the password on the dashboard; this is just an example -->
            <p><strong>Password:</strong> <span id="password"><%= user.password %></span></p>
        </div>
        <a href="/logout">Logout</a>
    </div>
</body>
</html>
